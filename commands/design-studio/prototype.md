---
description: Создать интерактивный web/mobile prototype с реальными состояниями и переходами. Use для кликабельного UX-прототипа.
argument-hint: "<сценарий, экраны, платформа, fidelity>"
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

Собери интерактивный прототип **$ARGUMENTS** ролью `design-studio`. Используй `$design-studio` и `built-in-skills/interactive-prototype.md`; для mobile также `mobile-prototype.md`. Реализуй ключевые состояния и переходы, сохрани в `designs/<project>/`, проверь сценарий в HTTP-preview на целевых viewport.
