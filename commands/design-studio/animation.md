---
description: Создать timeline/motion artifact и при необходимости экспортировать его в MP4. Use для explainer, walkthrough и animated visual.
argument-hint: "<сценарий, длительность, размер, звук, формат>"
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

Создай анимацию **$ARGUMENTS** ролью `design-studio` по `animated-video.md`; при звуке также используй `sound-effects.md`. Сначала проверь HTML-анимацию и тайминг в preview, затем экспортируй MP4 штатным `export-as-video.md`/`agents/gen-video` и проверь полученный файл.
