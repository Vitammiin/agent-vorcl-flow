---
description: План безопасной обратимой миграции схемы/данных. Use when нужно ИЗМЕНИТЬ схему или данные (DDL/backfill) — единственная команда с мутациями по подтверждению; чтение и аналитика — /database:query (database)
argument-hint: "<что мигрируем>"
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

Спланируй и подготовь безопасную миграцию: **$ARGUMENTS**.

Только **обратимые** миграции по схеме **expand → migrate/backfill → contract** (zero-downtime): сначала совместимое добавление, затем перелив данных батчами, затем удаление старого отдельным шагом. Большие таблицы — без долгих блокировок (`CREATE INDEX CONCURRENTLY`, батч-бэкфилл). Обязательно предусмотри `down`/откат.

1. Изучи текущую схему (read-only через MCP) и код миграций; напиши миграцию (up/down).
2. **Покажи план изменений**: DDL, порядок шагов, оценка влияния — какие блокировки и на сколько, ожидаемое время, обратимость (что делает `down`).
3. **Дождись ЯВНОГО подтверждения человека** — слова вроде «да, применяй»; молчание или «ок» на другой вопрос — НЕ подтверждение. Для prod необратимые активации выноси в PR.
4. Применяй, затем **проверь результат и покажи доказательство**: схема после (`information_schema`/`listCollections`), статус миграции, `EXPLAIN` затронутых запросов, что `down` существует и валиден.

Первопричину чини схемой/кодом, а не точечной правкой записей. Опирайся на навыки `database`, `postgresql`, `mongodb`. Делегируй субагенту `database` (для доступа к прод-БД на Render — агенту `render`).
