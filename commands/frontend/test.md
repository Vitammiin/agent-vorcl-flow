---
description: Генерация тестов компонентов (frontend)
argument-hint: "<что тестируем>"
allowed-tools: Read, Write, Edit, Bash
---

Напиши тесты для: **$ARGUMENTS**.

Поведенческие тесты по скиллу `react-testing`: Testing Library + Vitest, поиск по ролям/тексту, взаимодействие через `user-event`, структура AAA. Мокай границу сети через MSW; давай свежий `QueryClient` на тест (`retry: false`). Покрой happy path и состояния loading/empty/error, крайние случаи. Прогони тесты и покажи зелёный вывод. Опирайся на навыки `react-testing`, `react`. При необходимости делегируй субагенту `frontend`.
