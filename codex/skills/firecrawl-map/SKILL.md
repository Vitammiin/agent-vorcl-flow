---
name: firecrawl-map
description: Быстрая карта URL сайта через Firecrawl перед crawl.
---

# Firecrawl map

CLI map после `--help` → MCP → REST `/v2/map`. Сгруппируй URL, выдели нужные страницы, сохрани карту в `.firecrawl/`. Не запускай crawl автоматически.
