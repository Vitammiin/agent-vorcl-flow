---
name: mermaid-validate
description: Проверить Mermaid — синтаксис + реальный рендер-тест, найти и устранить ошибки (mmdc / Maid / mcp-mermaid) (роль mermaid). Use когда нужно убедиться, что диаграмма валидна и рендерится.
---

# Задача: валидация Mermaid

Проверь Mermaid на валидность и почини (см. `$mermaid-rendering`, `$mermaid-diagrams`).

1. **Линт:** `npx -y @probelabs/maid <путь>` — опечатки заголовка (`lowchart`), непарные `subgraph/end`, `end`-ловушка, неэкранированные подписи.
2. **Эталонный рендер-тест:** `mcp-mermaid` или `npx -p @mermaid-js/mermaid-cli mmdc -i <файл> -o /tmp/_check.svg` — единственная надёжная проверка.
3. Читай сообщения парсера, устрани каждую ошибку (кавычки, зарезервированные слова, направление, кардинальности), повтори рендер до зелёного.

Дай: найденные проблемы → правки → финальный зелёный рендер-тест (команда + результат).
