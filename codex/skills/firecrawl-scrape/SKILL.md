---
name: firecrawl-scrape
description: Скрейп одного известного URL через Firecrawl в markdown или JSON.
---

# Firecrawl scrape

CLI scrape после `--help` → MCP → REST/keyless client. По умолчанию основной markdown; для структуры JSON Schema. Кэш отключай только ради свежести, enhanced proxy — после обычной попытки. Сохрани в `.firecrawl/` и верни URL.
