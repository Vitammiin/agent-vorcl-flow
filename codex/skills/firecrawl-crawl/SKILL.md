---
name: firecrawl-crawl
description: Рекурсивный обход раздела/сайта через Firecrawl (роль firecrawl). Use when нужен контент МНОГИХ страниц раздела/сайта (дорого, всегда с limit); одна страница → $firecrawl-scrape, только список URL → $firecrawl-map.
---

# Задача: краул сайта (firecrawl)

Обойди сайт/раздел и собери контент.

Если объём неизвестен — сначала прикинь охват через `firecrawl_map`. Затем через MCP `firecrawl` запусти `firecrawl_crawl` **всегда** с разумным `limit` и ограничением по путям. Краул асинхронный: опрашивай `firecrawl_check_crawl_status` до готовности. `formats: ["markdown"]`, `onlyMainContent`. Большой краул дорог (кредиты) и медлен — не запускай без лимита; перед большим обходом (limit > 50 или сайт целиком) назови ожидаемый объём страниц и **дождись явного подтверждения человека**. Верни собранные страницы (markdown) — у каждой её URL-источник — и краткую сводку. Опирайся на `$web-scraping`.
